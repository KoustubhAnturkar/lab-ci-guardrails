# CI Guardrails Lab

This small HTTP service is the starter repository for the CI guardrails lab.  

Open the repository in its devcontainer, then establish the baseline:

```console
npm ci
npm test
```

The course lab writeup contains the tasks and submission instructions.
