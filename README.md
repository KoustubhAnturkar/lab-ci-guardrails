# GitHub Actions CI Lab

This small HTTP service is the starter repository for the GitHub Actions CI lab.

Open the repository in its devcontainer, then establish the baseline:

```console
npm ci
npm test
```

The course lab write-up explains how to turn the supplied project checks into a GitHub Actions workflow.
